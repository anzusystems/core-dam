#!/bin/bash

# select count(dam_media_api_mig.media_api_id) from dam_media_api_mig WHERE media_api_id < 600000;

cd /var/www/html

bin/console anzu:media-api:migrate --stages=migrateImages 0 600000 --no-debug -vv