#!/bin/bash

# select count(dam_media_api_mig.media_api_id) from dam_media_api_mig WHERE media_api_id >= 7800000 and media_api_id < 8400000; -- 300537

cd /var/www/html

bin/console anzu:media-api:migrate --stages=migrateImages 7800000 8400000 --no-debug