#!/bin/bash

# select count(dam_media_api_mig.media_api_id) from dam_media_api_mig WHERE media_api_id >= 6100000 and media_api_id < 6700000;

cd /var/www/html

bin/console anzu:media-api:migrate --stages=migrateImages 6100000 6700000 --no-debug