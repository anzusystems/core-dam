framework:
    cache:
        default_redis_provider: CacheRedis
        app: cache.adapter.redis
        system: cache.adapter.redis
        pools:
            doctrine.redis_cache_pool:
                adapter: cache.adapter.redis
                provider: CacheRedis
            core_dam_app.cache:
                adapter: cache.adapter.filesystem

