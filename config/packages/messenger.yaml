framework:
    messenger:
        transports: ~
        buses:
            messenger.bus.default:
                middleware:
                    Anzu\CommonBundle\Messenger\Middleware\ContextIdentityMiddleware

        routing: ~